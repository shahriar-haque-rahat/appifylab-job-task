# -----------------------------------------------
# Backend environment
# -----------------------------------------------

NODE_ENV=development
PORT=8000

# --- Database (PostgreSQL) ---
DATABASE_URL="postgresql://appifylab:appifylab@localhost:5432/appifylab?schema=public"
# (e.g. Neon). Leave blank locally — DATABASE_URL is used for both.
# DIRECT_URL=

# --- Redis ---
REDIS_URL="redis://localhost:6379"

# --- Auth / crypto ---
JWT_ACCESS_SECRET="change-me-to-a-long-random-string-at-least-32-chars"
ACCESS_TOKEN_TTL=15m
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_DAYS=7
BCRYPT_COST=12

# --- CORS / cookies ---
CORS_ORIGIN="http://localhost:3000"
TRUST_PROXY=0

# --- Cloudinary (optional) ---
UPLOAD_STORAGE=auto
UPLOAD_TIMEOUT_MS=15000
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# --- Misc ---
PUBLIC_BACKEND_URL="http://localhost:8000"
FEED_CACHE_TTL_SECONDS=30
FEED_PAGE_SIZE=10
MAX_IMAGE_BYTES=5242880

# --- Google OAuth (optional — "Sign in with Google") ---
GOOGLE_CLIENT_ID=
